DATABASE_URL="postgresql://fcsquad:fcsquad@localhost:5432/fc_squad_builder"

# Optional — only used as a fallback source when a player/club isn't found
# locally (see CLAUDE.md §3.4). The app works fine without them.
API_FOOTBALL_KEY=""
THESPORTSDB_KEY=""

# Optional — powers the "upload from computer" button next to photo/badge
# URL fields (src/app/api/upload/route.ts). Auto-set by Vercel once a Blob
# store is attached to the project; without it, those fields still work
# fine as plain URL inputs, just without the upload button.
BLOB_READ_WRITE_TOKEN=""
